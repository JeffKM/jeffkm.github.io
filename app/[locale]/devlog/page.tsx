import type { Metadata } from "next";
import { PageShell } from "@/components/page-shell";
import { isLocale } from "@/lib/content";
import { pageMetadata } from "@/lib/metadata";

export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};return pageMetadata(locale,locale==="ko"?"개발 기록":"Devlog",locale==="ko"?"문제, 선택지, 검증을 남기는 개발 기록":"Development notes about problems, options, and validation","/devlog")}
export default async function Devlog({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))return null;return <PageShell locale={locale} currentPath="devlog"><section className="page-hero shell"><p className="eyebrow">Devlog</p><h1>{locale==="ko"?<>과정보다 선명한<br/><em>결정의 기록.</em></>:<>A record of decisions,<br/><em>not activity.</em></>}</h1><p>{locale==="ko"?"해결한 문제, 고려한 선택지, 구조와 검증 결과가 생겼을 때만 발행합니다.":"Published when there is a resolved problem, meaningful alternatives, an implementation structure, and evidence."}</p></section><section className="shell article-list"><article><div className="article-index">001</div><div><p className="eyebrow">Portfolio Platform · 2026.08</p><h2>{locale==="ko"?"2019년 Jekyll 블로그를 이중 언어 제품 포트폴리오로 재구축하기":"Rebuilding a 2019 Jekyll blog as a bilingual product portfolio"}</h2><p>{locale==="ko"?"정보 구조, 정적 라우팅, 콘텐츠 근거와 모션 접근성을 하나의 배포 흐름으로 묶은 과정입니다.":"How information architecture, static routing, evidence-based content, and accessible motion became one deployable system."}</p><span className="draft-label">{locale==="ko"?"구현 검증 후 발행 예정":"Publishing after implementation verification"}</span></div></article></section></PageShell>}
